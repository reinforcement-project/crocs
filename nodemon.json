{
  "watch": ["server/**/*.ts"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "NODE_ENV=development ts-node -r dotenv/config ./server/server.ts --files --project tsconfig.server.json"
}
